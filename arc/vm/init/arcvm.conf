# Copyright 2018 The Chromium OS Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

description   "Start arcvm"
author        "chromium-os-dev@chromium.org"

# TODO(yusukes): Consider reusing crostini components and remove this job.
script
  # TODO(yusukes): Fix run-arcvm and remove the remount. Currently, run-arcvm
  # tries to modify .qcow file on the rootfs.
  mount -o remount,rw /
  /opt/google/vms/android/run-arcvm | logger -t "${UPSTART_JOB}"
end script
